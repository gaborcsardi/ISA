import(Category)
import(AnnotationDbi)

export(isa.in.silico, normalize, isa, isa.merge,
       isa.fix.oscillation, isa.step, isa.unique, generate.seeds)

export(robustness, isa.filter.robust, ROC, ROC.integral)

export(isa.GO, isa.KEGG, isa.CHR, isa.miRNA, isa.DBD)

export(gograph, gograph.plot, exp.plot.create, exp.plot.colbar,
       exp.plot, cond.plot, overlap, overlap.plot, mnplot, ISAmnplot,
       ISA2heatmap)
       
export(autogen.table, autogen.modules)

export(ppa.in.silico, ppa.normalize, ppa, ppa.step, ppa.unique)

##########

useDynLib(isa)
